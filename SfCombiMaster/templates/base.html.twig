<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>

<h2>Calcul de la factorielle</h2>
<form action="{{ path('app_home') }}" method="GET">
    <label for="factorial_n">n :</label>
    <input type="number" id="factorial_n" name="n_fact" min="0" value="{{ n_fact is defined ? n_fact : '' }}" required>
    <button type="submit">Calculer Factorielle</button>
</form>

<h2>Calcul des combinaisons</h2>
<form action="{{ path('app_home') }}" method="GET">
    <label for="combination_n">n :</label>
    <input type="number" id="combination_n" name="n_comb" min="0" value="{{ n_comb is defined ? n_comb : '' }}" required>
    <label for="combination_p">p :</label>
    <input type="number" id="combination_p" name="p" min="0" value="{{ p is defined ? p : '' }}" required>
    <button type="submit">Calculer Combinaisons</button>
</form>

<h2>Résultat</h2>
{% if result is defined and result is not null %}
    {% block resultat %}
    {% endblock %}
{% endif %}
</body>
</html>
